import styled from "styled-components"

export const SCCTA = styled.button`
	padding: 1.5rem 2rem;
	border: none;
	border-radius: 5px;
	font-weight: 600;
	background: var(--color-black);
	font-family: inherit;
	font-size: 1.6rem;
	color: var(--color-white);
	letter-spacing: 0.4px;
	cursor: pointer;

	&:hover {
		background: var(--bg-button-hover);
	}
`
