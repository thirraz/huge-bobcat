import styled from "styled-components"

export const SCReasons = styled.section`
	max-height: 100dvh;
	height: 100dvh;

	display: flex;
	flex-direction: column;
	align-items: center;
	gap: max(6rem, 10vh);
	padding-inline: var(--padding-desktop);
	margin-top: min(10rem, 20vh);
`
