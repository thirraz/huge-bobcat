import styled from "styled-components"

export const SCP = styled.p`
	color: ${props => props.color};
	margin-block: ${props => props.marginTop} 4rem;
	font-size: 1.6rem;
	font-weight: 500;
	line-height: 150%;
`
